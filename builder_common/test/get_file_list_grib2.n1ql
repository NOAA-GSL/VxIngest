            SELECT url, mtime
            FROM `mdatatest`._default.METAR
            WHERE
            subset='metar'
            AND type='DF'
            AND fileType='grib2'
            AND originType='model'
            AND model='HRRR_OPS' order by url;
